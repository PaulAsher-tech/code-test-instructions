# API Base URL for production builds
# In development, Vite proxy handles /api requests
# Set this when building for production if frontend is served from different origin
VITE_API_BASE_URL=http://localhost:8080