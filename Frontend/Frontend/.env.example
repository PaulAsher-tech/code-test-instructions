# API Base URL for production builds
# In development, Vite proxy handles /api requests automatically
# Set this when building for production if frontend is served from different origin than backend
# Example: VITE_API_BASE_URL=http://localhost:8080
# Leave empty or unset for same-origin requests
VITE_API_BASE_URL=